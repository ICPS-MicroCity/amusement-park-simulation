module org:protelis:microcity:satisfaction

import microcity.Queues.dequeue
import microcity.Queues.satisfactionUnion

share (field <- dequeue()) {
    foldHoodPlusSelf(field, { a, b -> satisfactionUnion(a, b) })
}
