module org:protelis:microcity:positions
import microcity.Device.getID
import microcity.Positions.createPositions
import microcity.Positions.union

share (field <- createPositions(getID().getId())) {
    foldHoodPlusSelf(field, (a, b) -> { union(a, b) })
}